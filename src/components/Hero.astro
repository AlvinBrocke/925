---
import { Image } from 'astro:assets';
import ChromeField from './ChromeField.astro';
import Countdown from './Countdown';
import logoMark from '../assets/logo-mark-vertical.png';
import lockup from '../assets/social-logo-lockup.png';
import { nextShift, revealAt } from '../data/shift';

const opensAt = revealAt(nextShift).toISOString();
---
<section class="hero" id="on-shift">
  <div class="bg" aria-hidden="true"></div>
  <div class="scan" aria-hidden="true"></div>

  <ChromeField field="hero" />

  <Image src={logoMark} alt="" aria-hidden="true" class="mark mark-a" width={130} densities={[1, 2]} />
  <Image src={logoMark} alt="" aria-hidden="true" class="mark mark-b" width={100} densities={[1, 2]} />
  <Image src={lockup} alt="" aria-hidden="true" class="mark mark-c" width={180} densities={[1, 2]} />

  <div class="content">
    <p class="eyebrow">— A cultural override. clock in. —</p>
    <h1>THE SHIFT<br />CONTINUES</h1>
    <p class="lede">
      The workday ends. The real shift starts. Location drops when the clock hits
      zero — after hours, on time.
    </p>

    <!-- client:load, not client:idle: the clock has to be ticking the moment it
         is on screen, and it is above the fold. -->
    <Countdown client:load revealAt={opensAt} />

    <div class="actions">
      <button class="btn btn-primary" data-clock-in>Clock In</button>
      <a class="btn btn-ghost" href="#shift-log">See Lineup</a>
    </div>
    <p class="handle">@925</p>
  </div>
</section>

<style>
  .hero {
    position: relative; min-height: 760px; display: flex; flex-direction: column;
    align-items: center; justify-content: center; overflow: hidden; padding: 60px 20px;
  }
  .bg { position: absolute; inset: 0; background: radial-gradient(circle at 50% 20%, var(--duotone-green-deep), #000 65%); }
  .scan {
    position: absolute; inset: 0; opacity: .14;
    background-image: repeating-linear-gradient(0deg, var(--teal) 0px, var(--teal) 1px, transparent 1px, transparent 4px);
    animation: scan 6s linear infinite;
  }
  .hero :global(.mark) { position: absolute; z-index: 1; height: auto; }
  .hero :global(.mark-a) { top: 8%; left: 6%; filter: drop-shadow(0 0 24px var(--teal-glow)); animation: floatA 7s ease-in-out infinite; }
  .hero :global(.mark-b) { bottom: 10%; right: 8%; filter: drop-shadow(0 0 20px var(--teal-glow)); animation: floatB 8s ease-in-out infinite; }
  .hero :global(.mark-c) { top: 14%; right: 5%; opacity: .9; filter: drop-shadow(0 0 18px #00000099); animation: floatC 9s ease-in-out infinite; }

  .content {
    position: relative; z-index: 2; text-align: center;
    display: flex; flex-direction: column; align-items: center; gap: var(--space-5);
  }
  .eyebrow {
    font-family: var(--font-mono); font-size: var(--text-xs);
    letter-spacing: var(--tracking-widest); color: var(--teal); text-transform: uppercase;
  }
  h1 {
    font-family: var(--font-display); font-weight: 900; font-size: var(--text-5xl);
    line-height: .95; letter-spacing: .02em; text-shadow: 0 0 40px #00d2c344;
  }
  .lede { font-size: var(--text-md); color: var(--chrome-300); max-width: 520px; }
  .actions { display: flex; gap: 14px; flex-wrap: wrap; justify-content: center; margin-top: var(--space-2); }
  .btn {
    font-family: var(--font-display); font-weight: 700; font-size: 17px;
    letter-spacing: var(--tracking-wider); text-transform: uppercase;
    padding: 16px 32px; border: 1px solid transparent; border-radius: var(--radius-sm);
    cursor: pointer; text-decoration: none; display: inline-block;
    transition: all var(--duration-base) var(--ease-standard);
  }
  .btn-primary { background: var(--accent); color: var(--accent-contrast); box-shadow: var(--shadow-glow-sm); }
  .btn-primary:hover { background: var(--accent-hover); }
  .btn-ghost { background: transparent; color: var(--chrome-100); border-color: var(--border-hairline); }
  .btn-ghost:hover { border-color: var(--teal); color: var(--teal); }
  .handle { font-family: var(--font-mono); font-size: var(--text-xs); color: var(--chrome-700); letter-spacing: .1em; }

  @media (max-width: 767px) {
    .hero { min-height: 620px; padding: 48px 20px; }
    h1 { font-size: var(--text-3xl); }
    .lede { font-size: var(--text-base); }
    .hero :global(.mark-c) { display: none; }
  }
</style>
