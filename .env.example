# The address is deliberately NOT in the repo or the client bundle. It is read
# only inside src/pages/api/location.ts, which refuses to return it until the
# reveal window opens. Set this in the Vercel dashboard for production.
SHIFT_LOCATION="Venue name, Street, Accra"

# Email service for the RSVP list. See src/pages/api/rsvp.ts.
RESEND_API_KEY=""
RSVP_AUDIENCE_ID=""
